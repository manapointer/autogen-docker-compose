name: Autogen Docker Compose
description: Generates Docker Compose configurations from CircleCI executor definitions
author: https://github.com/manapointer
requirements: []
configuration:
  properties:
    input_path:
      type: string
    output_path:
      type: string
    executor:
      type: string
    parameters:
      type: string
  required:
    - input_path
    - output_path
    - executor
